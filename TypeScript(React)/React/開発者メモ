node server.jsとnpm run build,npm startでフロントエンド、バックエンドのターミナルを開く




code: 'EADDRINUSE',{
errno: -48,
syscall: 'listen',
address: '::',
port: 9000
}
ポート番号9000番が使用されていた
lsof -i :9000

hashizumetoshiki@gensei7hashizume React % lsof -i :9000

COMMAND PID USER FD TYPE DEVICE SIZE/OFF NODE NAME
com.docke 46334 hashizumetoshiki 189u IPv6 0x36ec9ec4f88d3433 0t0 TCP *:cslistener (LISTEN)
hashizumetoshiki@gensei7hashizume React %

ポート9000を使っているプロセスがcom.dockeであり、PIDは46334だったので

kill -9 46334

ターミナルコマンドで削除
Container react-react-app-1
✔ Container react-backend-1
✔ Container react-mariadb-1
✔ Network react_react-network

ターミナルからDocker(mariadb)にログイン
docker exec -it 74a052f13b68 /bin/bash
パス wp10-root

MyAQL(mariadb)データベース入力項目

CREATE TABLE IF NOT EXISTS users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL
);
-- データベースの作成
CREATE DATABASE IF NOT EXISTS mydatabase;

-- データベースの選択
USE mydatabase;

-- ユーザーテーブルの作成
CREATE TABLE IF NOT EXISTS users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL
);

-- TODOテーブルの作成
CREATE TABLE IF NOT EXISTS todos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    text VARCHAR(255) NOT NULL,
    completed BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);

http://127.0.0.1:8080/index.php?route=/&route=%2F
phpmyadminユーザ名　パスワード
ユーザー名: myuser
パスワード: mypassword
